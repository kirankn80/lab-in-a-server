---
- name: Run playbooks for CENTOS
  include_tasks: centos.yml
  when: ansible_facts['distribution']=="CentOS"
- name: Run playbooks for Ubuntu
  include_tasks: ubuntu.yml
  when: ansible_facts['distribution']=="Ubuntu"  
